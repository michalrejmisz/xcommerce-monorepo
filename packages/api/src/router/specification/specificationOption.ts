import { z } from "zod";
import { SpecificationOption } from "@acme/db";
import { createTRPCRouter, protectedProcedure, publicProcedure } from "../../trpc";

export const specificationOptionRouter = createTRPCRouter({
});