import { z } from "zod";
import { ProductSpecificationTemplate } from "@acme/db";
import { createTRPCRouter, protectedProcedure, publicProcedure } from "../../trpc";

export const productSpecificationTemplateRouter = createTRPCRouter({
});